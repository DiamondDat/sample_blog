<%= form_for [post, @comment] do |f| %>
  <p>
    <%= f.label :body, "Your Comment" %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Comment' .  method="post", class: 'btn btn-primary' %>
  </p>
<% end %>
